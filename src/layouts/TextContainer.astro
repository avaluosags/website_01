---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---
<div class={`flex flex-col gap-y-8 px-8 md:px-40 py-14 xl:py-44 border-t border-primary_super ${className}`}>    
    <slot />    
</div>